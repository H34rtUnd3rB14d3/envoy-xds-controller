apiVersion: envoy.kaasops.io/v1alpha1
kind: VirtualService
metadata:
  name: dpl-vs-a
  annotations:
    envoy.kaasops.io/node-id: test
spec:
  listener:
    name: dpl-a
  httpFilters:
    - name: envoy.filters.http.router
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
  virtualHost:
    name: dpl-vh-a
    domains:
      - dup.kaasops.io
    routes:
      - match:
          prefix: "/"
        direct_response:
          status: 200
          body:
            inline_string: "from-listener-a"
